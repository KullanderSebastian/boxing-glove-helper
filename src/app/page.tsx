import React from "react";
import MainPage from "./components/MainPage";

const Page: React.FC = () => {
    return (
        <div>
            <MainPage />
        </div>
    );
};

export default Page;